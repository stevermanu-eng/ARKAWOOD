{
  "name": "arka-wood-web",
  "version": "0.5.0",
  "private": true,
  "description": "Sitio de ARKA WOOD y sistema de postulaciones sobre Cloudflare Pages Functions.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npx wrangler pages dev dist",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "type": "module"
}
